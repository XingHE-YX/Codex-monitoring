# Codex 重置预警——Android 应用流程

**文档状态：** 已批准实施

**平台：** v1 仅支持 Android

**主要设备：** 搭载 Google Play 服务的一加 13

**导航样式：** 使用 Material 3 底部导航栏，包含四个目的地：首页、证据、运行记录、设置

## 1. 全局导航模型

```text
冷启动
  ├─ 本地不存在设备凭据 → 配对
  └─ 本地存在设备凭据 → 首页

首页
  ├─ 点击当前预警 → 预警详情
  ├─ 点击“查看证据” → 预警详情
  ├─ 点击“立即检查” → 手动检查进度 → 首页
  ├─ 点击“证据” → 证据列表
  ├─ 点击“运行记录” → 运行历史
  └─ 点击“设置” → 设置

证据列表
  └─ 点击一条预警 → 预警详情

运行历史
  └─ 点击一条运行记录 → 运行详情

设置
  ├─ 点击“通知偏好” → 通知偏好
  ├─ 点击“连接状态” → 连接诊断
  ├─ 点击“测试通知” → 测试通知结果
  └─ 点击“重新配对设备” → 重新配对确认 → 配对

点击系统通知
  ├─ 预警 ID 有效 → 预警详情
  └─ 预警 ID 缺失或已过期 → 首页并显示错误消息
```

## 2. 启动路由

### 触发条件

Android 进程启动、启动器 Activity 被创建，或用户点击应用图标。

### 步骤

1. 从由 Android Keystore 支持的本地加密存储中加载设备凭据。
2. 加载缓存的首页状态以及上次成功同步时间戳。
3. 检查设备凭据是否存在且未在本地标记为已撤销。
4. 如果不存在凭据，跳转至配对页面。
5. 如果存在凭据，则向服务器请求当前首页状态。
6. 在网络请求进行期间，立即渲染缓存数据。

### 成功状态

- 已配对设备打开首页。
- 新安装的设备打开配对页面。
- 刷新期间缓存数据保持可见。

### 错误状态

- 如果无法连接服务器，已配对设备停留在首页，显示“离线”横幅和缓存数据。
- 如果服务器返回 `401`，仅在用户确认“重新配对设备”后清除本地凭据。
- 如果无法读取本地安全存储，显示包含“重置本地配对”操作的阻塞式恢复对话框，并说明必须重新配对设备。

## 3. 配对页面

### 目的

无需 Gmail 登录或通用账户系统，将唯一的 Android 应用实例绑定到私有监测服务。

### 触发条件

设备没有有效的本地设备凭据，或用户选择“重新配对设备”。

### 可见内容

- 应用名称：Codex 重置预警
- 简短说明：“请输入服务器生成的一次性配对码。”
- 配对码输入框，接收八位大写字母或数字。
- “配对”按钮。
- 连接状态指示器。
- 错误消息区域。

### 步骤

1. 用户输入八位配对码。
2. 应用在发起请求之前验证本地格式。
3. 应用获取或刷新 FCM 注册令牌。
4. 应用将配对码、FCM 令牌、设备型号、Android 版本和应用版本发送至 `POST /api/v1/pair/claim`。
5. 服务器验证一次性配对码，创建唯一的活跃设备绑定，并返回不透明的设备访问令牌和当前首页状态。
6. 应用将访问令牌保存到本地加密存储中。
7. 应用向服务器注册 FCM 令牌，然后跳转至首页。

### 成功状态

- 配对码被标记为已使用。
- 设备获得稳定的设备 ID。
- 应用显示首页。
- 本地仅显示一次“配对完成”消息。

### 错误状态

- 格式无效：“配对码必须包含八位大写字母或数字。”不发起网络请求。
- 配对码已过期：“此配对码已过期。请在服务器上生成新的配对码。”
- 配对码已使用：“此配对码已被使用。”
- 尝试次数过多：“配对功能已暂时锁定，请稍后重试。”
- 已绑定设备：“已配对另一台设备。请先在服务器上撤销该设备，再配对本设备。”
- 网络故障：“无法连接服务器。请检查网络连接后重试。”
- 服务器错误：显示服务器返回的错误标题和重试操作；不得保存不完整的凭据。

## 4. 首页——正常状态

### 触发条件

用户完成配对、启动应用、点击“首页”，或点击不含有效预警 ID 的通知。

### 布局

1. Material 3 顶部应用栏，包含产品名称和设置图标。
2. 唯一的运行状态区域，包含：
   - 当前服务状态：“正常”或“异常”。
   - 上次检查时间。
   - 下次计划检查时间。
   - 北京时间时区标签。
   - “立即检查”按钮。
3. 显示“无预警”的紧凑状态卡片。
4. 三个数据源健康状态标签：X、额度预测和 OpenAI Status。
5. 可选的信息卡片，说明只有 A/B 级预警会触发通知。
6. Material 3 底部导航栏。

### 步骤

1. 立即渲染缓存数据。
2. 发起一次经过身份验证的 `GET /api/v1/home` 请求。
3. 更新运行状态区域、当前状态卡片和数据源状态标签。
4. 如果用户点击“立即检查”，则在服务器接受请求期间禁用该按钮。

### 成功状态

- 状态卡片显示“无预警”。
- 运行状态区域显示上次和下次检查时间。
- 每个数据源状态标签独立显示“正常”或“异常”。
- 应用不会为正常状态创建通知。

### 错误状态

- 部分数据源失败：保留当前预警结果，显示“异常”状态，并提供前往“运行记录”的链接。
- 服务器完全不可用：显示缓存数据和“离线”横幅。
- 手动检查触发频率限制：显示“最近已请求过检查，请在冷却时间结束后重试。”

## 5. 首页——预警状态

### 触发条件

服务器报告存在活跃的 A 级或 B 级预警。

### 布局

运行状态区域保持在同一位置。当前状态卡片变为预警卡片，且不包含长篇分析。

### 必需的四行摘要

```text
【A级 / B级】
证据：一句话
窗口：预计时间，使用北京时间
建议：集中消耗额度 / 继续观察
```

应用可以渲染本地化的简体中文文案，但载荷字段必须保持相同的四行结构。

### 步骤

1. 获取或接收活跃预警。
2. 显示级别颜色和四行摘要。
3. 显示“查看证据”操作。
4. 当用户点击卡片或“查看证据”时，打开预警详情。

### 成功状态

- 当前首页状态中可见该预警。
- 用户可以打开完整且精确的证据链。
- 预警显示建议是“集中消耗额度”还是“继续观察”。

### 错误状态

- 无法获取预警详情：显示缓存的四行摘要和重试操作。
- 预警已过期：显示历史预警详情，并标记为“不再活跃”。

## 6. 预警详情页面

### 触发条件

用户点击预警卡片、“查看证据”或 Android 系统通知。

### 布局

1. 顶部应用栏，包含返回导航和级别指示器。
2. 预警摘要卡片，包含级别、证据句、窗口、建议、首次发现时间和最后更新时间。
3. 证据链列表，先按来源权威性排序，再按采集时间排序：
   - X 原帖或公开回复上下文。
   - OpenAI Status 的 Codex 事件。
   - 额度预测观测结果。
4. 每条证据包含来源名称、发布时间、采集时间、引用文本、上下文文本和“打开来源”操作。
5. 当官方证据与预测概率不一致时，显示冲突卡片。
6. 显示提示：官方全局公告不保证个人账户额度同时完成同步。

### 步骤

1. 从导航参数中读取预警 ID。
2. 请求 `GET /api/v1/alerts/{alert_id}`。
3. 渲染服务器返回的摘要和全部证据。
4. 当用户点击“打开来源”时，使用系统浏览器打开规范的 HTTPS URL。
5. 用户返回应用时保留滚动位置。

### 成功状态

- 用户可以独立核验该预警的判定原因。
- 所有显示时间均包含北京时间或明确的数据源时区。
- 来源链接在应用外部打开。

### 错误状态

- 未知预警 ID：显示“此预警不可用”，并返回证据列表。
- 数据已过期：显示已存储的证据，并标记为“已归档”。
- 网络故障：渲染缓存证据并显示“离线”横幅。

## 7. 证据列表页面

### 触发条件

用户点击底部导航栏中的“证据”。

### 布局

- 筛选标签：全部、A级、B级、官方、预测冲突。
- 按时间顺序显示最近 30 天内的预警列表。
- 每个列表项显示级别、标题、窗口、最后更新时间以及活跃或已归档状态。
- 空状态：“最近 30 天内没有预警。”

### 步骤

1. 使用游标分页请求 `GET /api/v1/alerts`。
2. 渲染第一页。
3. 当列表滚动到底部时加载后续页面。
4. 如果所需字段已加载，则在本地应用筛选条件；否则在下一次请求中加入查询参数。
5. 点击列表项时跳转至预警详情。

### 成功状态

- 列表仅包含保留期为 30 天的记录。
- 筛选操作不会改变服务器的预警分类结果。

### 错误状态

- 结果为空：显示明确的空状态。
- 网络故障：显示缓存列表和重试操作。
- 游标无效：从第一页重新开始加载。

## 8. 运行记录页面

### 触发条件

用户点击底部导航栏中的“运行记录”。

### 布局

- 运行日期和北京时间的执行时刻。
- 运行结果：无预警、预警、部分失败或失败。
- X、额度预测和 OpenAI Status 的数据源状态摘要。
- “手动”或“计划”标签。

### 步骤

1. 使用 30 天日期范围请求 `GET /api/v1/runs`。
2. 优先渲染最新的运行记录。
3. 点击运行记录时打开运行详情。

### 成功状态

- 正常完成的运行记录显示“无预警”。
- 数据源失败必须在视觉上区别于无异常的正常运行记录。

### 错误状态

- 没有记录：显示“最近 30 天内没有运行记录。”
- 网络故障：显示缓存的运行记录和重试操作。

## 9. 运行详情页面

### 触发条件

用户在运行记录页面中点击一条运行记录。

### 布局

- 运行 ID、计划时间、开始时间、完成时间和持续时间。
- 扫描窗口的开始时间和结束时间。
- 各数据源的状态、条目数量和错误摘要。
- 本次运行创建或更新的预警 ID。

### 成功状态

用户可以区分“没有符合条件的信号”和“数据源或分类器失败”。

### 错误状态

如果运行记录已过期，则仅显示其缓存摘要，并标记为“已归档”。

## 10. 设置页面

### 触发条件

用户点击底部导航栏中的“设置”，或点击顶部应用栏的设置图标。

### 分区

1. 设备配对状态。
2. Android 推送和电子邮件通知偏好。
3. 连接诊断。
4. 固定数据源列表。
5. 时区与检查计划，v1 中为只读。
6. 应用版本和服务器版本。
7. 重新配对设备。

### 成功状态

- 通过 `PATCH /api/v1/preferences` 保存偏好设置。
- “测试通知”分别报告 FCM 和电子邮件的结果。

### 错误状态

- 通知权限已禁用：提供前往 Android 通知设置的链接。
- 偏好设置更新失败：恢复开关之前的状态并显示重试操作。
- 取消重新配对：返回设置页面，不更改当前凭据。

## 11. 重新配对流程

### 触发条件

用户选择“重新配对设备”并确认该操作。

### 步骤

1. 显示破坏性操作确认对话框，说明当前设备凭据将在本地被撤销。
2. 如果用户确认，则调用 `POST /api/v1/device/revoke`。
3. 删除本地加密凭据和缓存的 FCM 关联。
4. 跳转至配对页面。
5. 要求使用新生成的一次性配对码。

### 成功状态

旧设备令牌无法再访问受保护端点，应用显示配对页面。

### 错误状态

如果无法连接服务器，则不删除本地凭据，并提供重试或取消操作。

## 12. 后台通知流程

### 触发条件

服务器创建新的逻辑预警通知，或预警级别升级。

### 步骤

1. 服务器向已注册的 FCM 令牌发送通知载荷。
2. 当应用处于后台或正常关闭状态时，Android 在系统通知栏中显示通知。
3. 用户点击通知。
4. 应用从 Intent 附加数据中读取 `alert_id` 和 `event_type`。
5. 应用请求最新的预警详情并跳转至预警详情页面。

### 成功状态

- 通知标题明确区分 A 级确认和 B 级预测。
- 详情页面打开与通知匹配的预警，而不是仅打开首页。

### 错误状态

- 预警 ID 已过期：打开首页并显示“该预警已超出历史记录保留期限。”
- FCM 令牌无效：应用在下次启动时刷新令牌，并将其发送至服务器。
- Android 通知权限已禁用：服务器记录发送尝试，同时设置页面说明本地权限状态。
- Android 被强行停止：通知送达受 Android 系统行为约束，在用户重新打开应用之前无法保证送达。

## 13. 手动检查流程

### 触发条件

用户在首页点击“立即检查”。

### 步骤

1. 应用调用 `POST /api/v1/checks`。
2. 服务器验证设备令牌以及五分钟的手动检查频率限制。
3. 服务器将一次运行加入队列并返回运行 ID。
4. 应用将按钮文字更改为“检查中”。
5. 应用轮询 `GET /api/v1/runs/{run_id}`，最长持续 60 秒。
6. 运行完成后，应用刷新首页。

### 成功状态

- 运行结果为无预警、预警、部分失败或失败。
- 首页显示更新后的上次检查时间。

### 错误状态

- 触发频率限制：保持按钮可用，并显示冷却时间文案。
- 已有检查正在运行：如果服务器返回现有运行 ID，则关联到该运行。
- 超时：显示“检查仍在运行”，并允许用户前往运行记录页面查看。
