# Codex 重置预警——前端指南

**平台：** Android

**UI 工具包：** Kotlin + Jetpack Compose + Material Design 3

**默认主题：** 以薰衣草紫为主强调色的深灰黑色 Material 3 主题

**主要目标设备：** 一加 13 紧凑型移动端布局

## 1. 设计方向

视觉语言应当沉静、技术化，并以证据为核心。本应用应呈现为可靠的监控控制台，而不是社交信息流。

- 使用具有层次的灰黑色表面，不使用单一的纯黑色画布。
- 使用薰衣草紫作为主要交互强调色。
- 绿色仅用于健康或正常状态。
- 琥珀色用于 B 级预警，红色用于 A 级预警。
- 无预警时，首页应保持平静克制。
- 预警应在视觉上醒目，但不得使用大面积全屏横幅。
- 只设置一个运行状态区；不得在第二个工具栏中重复显示上次检查、下次检查或“立即检查”。
- 优先使用 Material 3 组件和语义化颜色角色，不使用临时拼凑的自定义控件。

## 2. Material 3 颜色系统

所有颜色均表示为 Compose `Color` 常量，并映射到 Material 3 角色。组件必须使用 `MaterialTheme.colorScheme` 角色，不得在页面代码中硬编码十六进制颜色值。

### 2.1 核心深色方案

| 角色 | 十六进制 | 用途 |
|---|---|---|
| `background` | `#1C1B1F` | 应用画布 |
| `onBackground` | `#E6E1E5` | 画布上的主要文本 |
| `surface` | `#1C1B1F` | 顶层表面 |
| `onSurface` | `#E6E1E5` | 表面上的文本 |
| `surfaceVariant` | `#49454F` | 低强调度容器和分隔线 |
| `onSurfaceVariant` | `#CAC4D0` | 次要文本和图标 |
| `surfaceContainerLowest` | `#141218` | 最深层卡片表面 |
| `surfaceContainerLow` | `#211F26` | 次级卡片表面 |
| `surfaceContainer` | `#25232A` | 默认卡片表面 |
| `surfaceContainerHigh` | `#2B2930` | 浮起卡片和导航表面 |
| `surfaceContainerHighest` | `#36343B` | 最高局部强调层级 |
| `primary` | `#D0BCFF` | 主要操作和激活的导航项 |
| `onPrimary` | `#381E72` | 主色表面上的文本或图标 |
| `primaryContainer` | `#4F378B` | 主色调容器 |
| `onPrimaryContainer` | `#EADDFF` | 主色调容器上的文本 |
| `secondary` | `#CCC2DC` | 次要控件 |
| `onSecondary` | `#332D41` | 次要色表面上的文本 |
| `outline` | `#938F99` | 边框和分隔线 |
| `outlineVariant` | `#49454F` | 低强调度边框 |
| `inverseSurface` | `#E6E1E5` | 反色表面 |
| `inverseOnSurface` | `#322F35` | 反色表面上的文本 |

### 2.2 语义化状态颜色

语义化颜色映射到色调容器，以确保深色表面仍具有良好的可读性。

| 状态 | 主色 | 容器 | 容器前景色 | 用途 |
|---|---|---|---|---|
| 正常 | `#A8D5AE` | `#1F3725` | `#BCEFC0` | 服务健康，无预警 |
| 降级 | `#F7BD72` | `#4A2F00` | `#FFDDB0` | 一个或多个来源检查失败 |
| B 级 | `#FFB77A` | `#5A2A00` | `#FFDBBF` | 存在歧义或由预测信号支持的未来额度信号 |
| A 级 | `#FFB4AB` | `#690005` | `#FFDAD6` | 官方明确承诺在未来 24 小时内执行 |
| 错误 | `#FFB4AB` | `#93000A` | `#FFDAD6` | 请求失败或身份验证失败 |

如果某次运行存在部分失败，不得使用绿色暗示所有数据源均已成功获取；此时应使用“降级”状态。

## 3. 字体排印

### 3.1 字体族

- 主要拉丁字体：Roboto。
- 简体中文回退字体：Noto Sans CJK SC，其次使用系统无衬线字体。
- 监控内容不得使用装饰性字体。
- `FontWeight.Bold` 仅用于严重级别、页面标题和主要结果。

### 3.2 字体层级

| 令牌名称 | 字号 | 行高 | 字重 | 用途 |
|---|---:|---:|---:|---|
| `displaySmall` | 36sp | 44sp | 常规 | 少量使用的空状态插图标题 |
| `headlineLarge` | 32sp | 40sp | 常规 | 除非确有必要，否则避免在紧凑型屏幕上使用 |
| `headlineMedium` | 28sp | 36sp | 常规 | 详情页主标题 |
| `titleLarge` | 22sp | 28sp | 中等 | 页面标题 |
| `titleMedium` | 16sp | 24sp | 中等 | 卡片标题 |
| `titleSmall` | 14sp | 20sp | 中等 | 分区标题 |
| `bodyLarge` | 16sp | 24sp | 常规 | 重要证据文本 |
| `bodyMedium` | 14sp | 20sp | 常规 | 普通正文 |
| `bodySmall` | 12sp | 16sp | 常规 | 元数据和时间戳 |
| `labelLarge` | 14sp | 20sp | 中等 | 填充按钮标签 |
| `labelMedium` | 12sp | 16sp | 中等 | 标签块和紧凑型控件 |
| `labelSmall` | 11sp | 16sp | 中等 | 辅助状态标签 |

正文最小字号为 12sp。必要信息的透明度不得低于 60%。

## 4. 间距与尺寸

使用 4dp 基础网格。允许的间距值为 `4, 8, 12, 16, 20, 24, 28, 32, 40, 48` dp。

| 令牌名称 | 数值 | 用途 |
|---|---:|---|
| `space1` | 4dp | 图标与标签之间的微小间距 |
| `space2` | 8dp | 标签块间距、元数据间距 |
| `space3` | 12dp | 紧凑型卡片内部间距 |
| `space4` | 16dp | 标准内容内边距 |
| `space5` | 20dp | 分区间距 |
| `space6` | 24dp | 页面水平边距和主要分区间距 |
| `space7` | 32dp | 大屏幕分区间距 |
| `space8` | 40dp | 空状态留白 |

尺寸：

- 紧凑型页面水平内边距：16dp。
- 紧凑型卡片水平内边距：16dp。
- 标准触控目标：最小 48dp。
- 图标尺寸：24dp；紧凑型前导图标容器：40dp。
- 顶部应用栏：64dp，另加系统内边距。
- 导航栏：使用具备系统内边距处理能力的 Material 3 `NavigationBar`。
- 主要按钮高度：至少 48dp。
- 文本字段最小高度：56dp。
- 卡片最小圆角半径：16dp。

## 5. 形状与高度层级

| 形状令牌 | 数值 | 用途 |
|---|---:|---|
| `extraSmall` | 4dp | 小型菜单和紧凑元素 |
| `small` | 8dp | 文本字段和小型标签块 |
| `medium` | 12dp | 按钮和列表项 |
| `large` | 16dp | 标准卡片 |
| `extraLarge` | 28dp | 预警卡片和模态容器 |
| `full` | 50% | 状态标签块和图标按钮 |

使用 Material 3 色调高度层级和表面容器。深色主题中应避免浓重阴影。选中或紧急状态应通过颜色角色、边框和图标表达，不得依赖大面积阴影。

## 6. 组件清单

### 6.1 应用框架

- 使用包含 `TopAppBar`、内容插槽和 `NavigationBar` 的 `Scaffold`。
- 必须遵循 `WindowInsets.safeDrawing`。
- 顶部应用栏仅包含产品名称和“设置”图标。
- 运行状态应位于首页内容中，不得放在顶部应用栏内。

### 6.2 运行状态区

以下内容只能出现在此区域：

- 当前服务状态。
- 上次检查时间。
- 下次检查时间。
- 北京时间时区标签。
- “立即检查”按钮。

正常状态使用绿色色调容器，降级状态使用琥珀色色调容器。“立即检查”按钮使用填充色调按钮或主按钮；请求被接受后，按钮进入禁用状态并显示进度指示器。

### 6.3 首页状态卡片

- 无预警：使用带有 `surfaceContainer`、绿色勾选图标容器、标题和一句说明的 `Card`；除了可选的跳转到“运行记录”操作外，不提供其他操作。
- A/B 级预警：使用语义化严重级别容器、严重级别标签、四行摘要和“查看证据”按钮组成的 `Card`。
- 卡片不得包含过长的证据文本。
- 卡片必须适配紧凑型宽度，不得出现水平滚动。

### 6.4 证据卡片

- 使用垂直 `List` 展示证据卡片。
- 显示来源、来源类型、发布时间、采集时间、引文和来源操作。
- 引用文本使用 `SelectionContainer`。
- 较长的来源文本默认折叠，并提供明确的展开操作。
- 来源链接在系统浏览器中打开。

### 6.5 标签块

- 来源健康状态和筛选器使用 `AssistChip` 或 `FilterChip`。
- 健康状态标签块同时包含文字和状态图标；不得仅依赖颜色表达状态。
- 筛选标签块必须同时使用容器颜色和勾选图标展示选中状态。

### 6.6 按钮

- 主要操作：`Button` 或 `FilledTonalButton`。
- 次要操作：`OutlinedButton`。
- 小型上下文操作：`TextButton`。
- 破坏性操作：使用错误容器的 `Button`，并要求用户明确确认。
- “立即检查”“查看证据”或“打开来源”不得使用仅含图标的按钮。

### 6.7 反馈

- 短暂显示的网络或重试消息使用 `Snackbar`。
- 重新配对确认和不可逆操作使用 `AlertDialog`。
- 离线和降级状态使用常驻的 `Banner` 或 `Surface`。
- 骨架占位仅用于首次加载且本地无缓存时；刷新期间不得隐藏缓存数据。

## 7. 响应式规则

即使 v1 仅面向手机，也必须使用 Material 3 窗口尺寸类别。

| 窗口宽度 | 类别 | 规则 |
|---:|---|---|
| `< 600dp` | Compact | 单列内容、底部 `NavigationBar`、16dp 侧边内边距 |
| `600–839dp` | Medium | 单主窗格，内容最大宽度为 640dp；横屏或平板设备允许使用 `NavigationRail` |
| `>= 840dp` | Expanded | 证据列表—详情布局；使用 `NavigationRail`；内容最大宽度为 1200dp |

Compact 规则：

- 不得使用固定的桌面端宽度。
- 首页卡片宽度为 `maxWidth = parentWidth - 32dp`。
- 运行状态内容中的元数据可以换行至两行。
- “立即检查”按钮高度不得低于 48dp；宽度小于 360dp 时，可以移动到第二行。
- 证据引文文本自动换行；URL 可选择，并在安全边界处断行。
- 较长的中文和英文字符串必须在 1.3x 与 1.5x 字体缩放比例下进行测试。

## 8. 各页面布局规则

### 首页

从上到下的顺序：

1. 运行状态区。
2. 当前状态卡片。
3. 来源健康状态标签块。
4. 可选的信息卡片。
5. 底部导航栏。

不得显示第二个状态栏，也不得在卡片下方重复上次检查和下次检查的数值。

### 证据

筛选器位于顶部应用栏下方、列表上方。筛选器保持水平滚动，证据列表保持垂直滚动。

### 运行记录

使用紧凑型时间线或列表。运行状态必须明确区分“无预警”“部分失败”和“失败”。

### 设置

使用由 `ListItem` 分区标题分组的 `ListItem` 行。不得暴露密钥值、API 密钥、应用专用密码、服务账号内容，也不得将原始服务器 IP 放入可复制的密钥字段中。

## 9. 无障碍

- 每个仅含图标的控件都必须设置内容描述。
- 每种严重级别都必须同时使用文字、图标和颜色表达。
- 最小触控目标为 48dp。
- 在适用情况下，必要文本的对比度必须达到 4.5:1；大号文本必须达到 3:1。
- 不得仅通过绿色或红色传达健康状态。
- 屏幕阅读器必须先播报严重级别，再播报预警标题。
- 动态字号和字体缩放不得导致证据文本或按钮被裁切。
- 链接必须使用有意义的标签，不得只显示“点击这里”。
- 动效为可选增强，不得将动效作为理解状态转换的必要条件。

## 10. 动效

- 仅在能够改善导航体验时，使用 Material 3 默认的进入、退出和共享轴转场。
- 首页不得使用持续动画。
- 新预警可以使用一次轻微的状态转场；不得让整个屏幕闪烁。
- 预计耗时超过 300ms 的操作必须显示进度指示器。

## 11. 文案规则

- 面向用户的默认语言：简体中文。
- 正常且无预警时使用“无预警”。
- “显示正常”仅用于服务或来源健康状态，不得用于实际运行失败的情况。
- 某次运行至少有一个来源检查失败时，使用“部分失败”。
- 始终统一使用“A级”和“B级”；不得与“高危/中危”交替使用。
- 首页预警内容在“查看证据”操作之前最多显示四行。
- 时间窗口始终显示北京时间；必要时单独显示来源时间。
