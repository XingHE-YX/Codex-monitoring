# Codex 重置预警 — 产品需求文档

**文档状态：** 已确认，可进入实现

**版本：** 1.0.0

**日期：** 2026-08-12

**产品形态：** Android 单设备个人使用 App + 香港云端 Rust 监测服务

**Android 包名：** `com.xingheluqi.codexresetwatch`

## 1. 产品概述

Codex 重置预警是一款面向单个用户的 Android 监测 App。云端服务按北京时间每小时检查三个指定公开来源，识别“未来可能重置 Codex 使用额度”的可信信号，并仅在达到 A 级或 B 级时发送 Android 推送和 Gmail 邮件。

App 首页提供一眼可读的当前状态，证据页提供可复核的原帖、回复上下文、预测站变化和 OpenAI Status 事件。所有历史证据和运行记录只保留 30 天。

核心原则：

- 宁可漏报，也不因单独的模糊词、社区猜测或已发生事件制造误报。
- 官方原帖和直接上下文优先于预测站概率。
- App、Android 推送和邮件使用同一个云端判级结果。
- 默认黑色/灰黑 Material 3 视觉，不使用大面积纯黑造成的层次缺失。
- Rust 只负责监测、判级、同步和通知核心；Android UI 使用 Kotlin + Jetpack Compose + 原生 Material 3。

## 2. 产品愿景

在用户真正需要消耗 Codex 额度之前，提供一条低噪声、可追溯、以官方证据为优先的重置预警。

## 3. 用户与使用场景

### 3.1 目标用户

唯一目标用户是产品所有者本人。首版不提供多用户注册、团队权限、公开订阅或账号体系。

### 3.2 主要使用场景

1. 用户打开 App，快速确认当前是否存在 A/B 级预警。
2. 用户收到 Android 推送或邮件，判断是否立即集中消耗 Codex 额度。
3. 用户点击预警，查看原文、北京时间、判定理由和来源链接。
4. 用户在发现数据源异常时，查看最近运行记录，而不是把抓取失败误认为“无预警”。
5. 用户首次安装 App，通过一次性配对码绑定唯一设备；之后不重复登录。

## 4. 已确认范围

### 4.1 平台与部署

- 首发平台：Android。
- 测试设备：一加 13，设备具备 Google Play 服务。
- 云端服务器：香港地域、2 vCPU、4 GiB RAM、200 Mbps，非阿里云。
- 后端系统：Rust 服务部署在该服务器上。
- 不设置域名，使用固定公网 IPv4。
- Android 与 API 必须使用 HTTPS；不允许生产环境明文 HTTP。
- 直接安装签名 APK，不上架 Google Play。

### 4.2 数据源

严格只使用以下三个来源族：

1. X：`https://x.com/thsottiaux` 的新帖子、公开回复及同一回复链的直接上下文。
2. 预测站：`https://www.willcodexquotareset.com/`。允许跟随根域名到 `www` 的 HTTP 重定向。
3. OpenAI Status：`https://status.openai.com/` 中与 Codex 相关的公开状态、事件和更新。

不得把其他 X 账号、第三方新闻、社区帖子、StatusGator、Reddit、GitHub issue 或其他预测站作为本产品的判级来源。

### 4.3 通知渠道

- Android 系统推送：Firebase Cloud Messaging（FCM）。
- 邮件：发件人 `xingheluqi56@gmail.com`，收件人 `2331613886@qq.com`。
- Gmail 使用应用专用密码；主密码不得写入服务配置。
- App、推送和邮件使用相同的 `alert_id`，保证可追溯到同一条结果。

## 5. 监测时间与运行规则

### 5.1 时区

所有调度、窗口、发布时刻、预估时间和邮件展示统一使用 `Asia/Shanghai`（北京时间，UTC+08:00）。数据库内部时间使用 UTC 存储，API 返回带偏移量的 ISO 8601 时间。

### 5.2 定时运行

- 每天 08:00—23:00，每个整点执行一次，共 16 个计划运行点：08:00、09:00、……、23:00。
- 00:00—07:59 不执行计划监测。
- 当天 08:00 运行时，除正常增量检查外，补查前一自然日 00:00:00 至当天 08:00:00（左闭右开）的新增信息。
- 运行失败不得推进对应数据源的成功游标；下一次运行必须重试未完成窗口。
- 手动“立即检查”不受计划窗口限制，但仍受服务端限流；它会使用当前游标执行一次增量检查并更新游标。

### 5.3 首次启动基线

首次部署或数据库为空时，服务执行一次首次基线初始化（bootstrap）：

- 读取三个来源的当前公开状态。
- 只对发布时间位于“最近 24 小时”内且仍指向未来重置的内容进行判级。
- 更早的内容只入库为历史基线，不触发通知。
- 首次基线初始化成功后写入每个来源的游标，后续运行只处理新增或实质变化内容。

## 6. 判级规则

### 6.1 术语

- **官方额度信号：** `@thsottiaux` 的相关公开帖子/回复，或 OpenAI Status 的 Codex 相关事件中明确涉及使用限额（usage limits）、速率限制（rate limits）、配额（quota）、使用容量（usage capacity）、补偿、恢复或等价使用额度的内容。
- **同一回复链直接上下文：** 原帖与其直接回复、被回复内容以及为理解该回复所必需的同一线程上下文；不能跨越无关帖子拼接语义。
- **未来信号：** 事件发布时间之后仍可能发生的重置、补发、恢复、提高或补偿。已经完成的重置不是预警。
- **实质变化：** 出现新的官方证据、额度对象变得明确、预测窗口变化超过 1 小时、预测概率跨过 70% 阈值、判级从 B 级升为 A 级，或原信号被官方明确撤回。

### 6.2 A 级

满足以下全部条件才判定为 A 级：

1. 官方来源明确承诺未来 24 小时内将重置、补发、恢复或提高 Codex / ChatGPT Work 的使用限额、速率限制、配额或等价使用额度。
2. 额度对象在同一帖子中明确，或同一回复链的直接上下文已经无歧义明确。
3. 信号指向未来动作，而不是单纯描述已经发生的重置。

以下情况不得单独判为 A 级：只出现 `reset`；玩笑；产品发布；模型发布；没有明确额度对象的“明天重置（reset tomorrow）”或“周一重置（reset Monday）”。

### 6.3 B 级

满足以下任一情况即可判定为 B 级，但必须仍然指向未来可能的额度重置/补偿：

1. 预测站概率大于或等于 70%，并且存在至少一个独立官方额度相关信号。
2. 官方明确说未来某日会 reset，且同一回复链高度指向 Codex / ChatGPT Work 额度，但对象、范围或具体时间仍有歧义。
3. 官方确认 Codex 额度/容量故障，并暗示或承诺补偿，但尚未明确执行时间。

### 6.4 冲突处理

- 预测站概率与官方明确承诺冲突时，以可核验的官方原帖及直接上下文优先。
- 官方明确承诺不得因预测站概率低而取消预警。
- 预测站的概率本身永远不能单独触发通知。
- 冲突必须写入证据记录，并在 App 详情和邮件中显示。

### 6.5 忽略条件

以下内容不通知：

- 普通产品发布预告或宣传。
- 模型发布、模型更新或产品功能介绍，除非同时满足 A/B 额度规则。
- 无来源社区猜测。
- 单独的高概率数字。
- 已经发生的重置。
- 旧信号的重复抓取。
- 只有模糊 `reset` 字样而没有额度对象的内容。
- 数据源访问失败、解析失败或模型 API 失败本身。

### 6.6 判级输出约束

未达到 A/B 级：不发送通知。运行记录的摘要只能是：`无预警`，或内部状态使用 `partial_failure` / `failed`，不得把失败写成正常无预警。

达到 A/B 级时，App 卡片、FCM 通知和邮件摘要必须遵守四行格式：

```text
【A级 / B级】
证据：一句话
窗口：预计时间，优先换算为北京时间
建议：集中消耗额度 / 继续观察
```

邮件可以在四行摘要之后附加完整证据区；App 首页不直接展开长篇分析。

## 7. 功能需求

### 7.1 云端监测服务（P0）

- 按北京时间执行计划运行。
- 对三个来源执行增量抓取。
- 跟随 `www.willcodexquotareset.com` 的规范化重定向。
- 保存来源游标、内容哈希、发布时间和原始响应摘要。
- 把 X 原帖、公开回复、预测站读数和 Status 事件转换成统一证据模型。
- 使用 `gpt-5.6-terra` 的 OpenAI-compatible API 做规则预筛后的语义判级。
- 对模型返回做 JSON Schema 校验；无效结果不得触发通知。
- 记录每次运行、每个来源的健康状态和错误原因。
- 自动清理 30 天以前的证据、运行记录、通知 payload 和来源快照。

### 7.2 去重与升级（P0）

- 相同来源项目通过 `(source_id, external_id)` 去重。
- 内容无变化的重复抓取不得重新判级或通知。
- B 级升级到 A 级时发送一次升级通知。
- 同等级只有在出现实质变化时重新通知。
- 同一个 `alert_id + channel + event_type` 只能成功发送一次。
- Email 与 FCM 失败可重试，但重试不得生成新的逻辑预警。

### 7.3 Android App（P0）

- 首次启动进入配对页。
- 输入一次性配对码，成功后绑定唯一设备。
- 配对完成后保存设备访问凭据，不要求 Gmail 登录，不要求重复登录。
- 首页展示唯一运行状态区：显示正常/异常、上次检查、下次检查、北京时间和“立即检查”。
- 首页展示极简状态卡：无预警或最近一条 A/B 预警。
- 首页展示三个数据源状态标签。
- 底部导航：首页、证据、记录、设置。
- 点击预警进入证据详情。
- 点击 FCM 通知进入对应预警详情。
- App 无网络时显示缓存数据和离线状态，不清空最近结果。

### 7.4 Android 推送（P0）

- 使用 FCM token 绑定唯一设备。
- 正常后台/退出 App 后仍发送系统通知。
- A 级使用高重要性通知渠道，B 级使用默认重要性通知渠道。
- 通知点击后深链到对应预警详情。
- FCM token 刷新后自动同步服务端。
- Android 系统强制停止应用、关闭通知权限或厂商电池策略阻止后台通信时，App 必须在设置页提示用户；服务端不能绕过 Android 的强制停止限制。

### 7.5 邮件通知（P0）

- A 级主题：`[Codex 重置预警][确认] Codex 重置已宣布`。
- B 级主题：`[Codex 重置预警][预告] 可能即将重置`。
- 正文包含：四行摘要、原文、北京时间发布时间、判定依据、命中关键词、来源状态、原帖/事件链接、冲突说明和“官方公告不保证个人账户同步”的必要说明。
- 仅在达到 A/B 级或发生实质变化时发送。
- 邮件发送失败记录错误并重试，不改变判级结果。

### 7.6 历史与证据（P0）

- 证据和运行记录保留 30 天。
- 用户可以按日期查看运行记录。
- 用户可以查看每条预警的证据链和来源链接。
- 用户可以看到预测站概率变化以及是否与官方信号冲突。
- 过期数据自动清理，清理任务本身写入运行日志。

### 7.7 设置（P1）

- 显示配对状态、设备名称、App 版本和服务连接状态。
- 显示通知开关：Android 推送、邮件通知。
- 提供“发送测试通知”按钮。
- 显示固定的数据源清单；首版不允许用户新增来源或修改 A/B 阈值。
- 显示服务器时间与北京时间偏移校验结果。

## 8. 邮件内容规范

### 8.1 A 级模板

```text
主题：[Codex 重置预警][确认] Codex 重置已宣布

检测到 Codex 重置确认动态。

【A级】
证据：官方明确表示未来 24 小时内将重置 Codex / ChatGPT Work 使用限额。
窗口：预计北京时间 YYYY-MM-DD HH:mm 前后
建议：集中消耗额度

原文：
<引用的来源原文>

发布时间（北京时间）：YYYY年M月D日 HH:mm:ss
判定依据：<具体的 A 级判定理由>
命中关键词：<命中的关键词>
来源状态：<来源抓取与上下文状态>
原帖链接：<规范来源网址>
说明：官方公告代表公开额度动作，不保证每个个人账户同时完成同步。
```

### 8.2 B 级模板

```text
主题：[Codex 重置预警][预告] 可能即将重置

检测到 Codex 未来额度重置的可信预警。

【B级】
证据：<一句话综合证据>
窗口：<北京时间或“未明确”>
建议：继续观察

预测站概率：<概率值或“未提供”>
官方信号：<来源摘要>
冲突说明：<“无”或具体冲突>
原帖/事件链接：<规范来源网址>
```

## 9. 非功能需求

- 可用性目标：计划运行成功率每 30 天不少于 99%。
- 单次计划运行目标：从开始抓取到写入结果不超过 5 分钟；模型 API 超时不超过 45 秒。
- 从检测到通知提交目标：判级完成后 60 秒内完成 FCM 和 SMTP 提交。
- 服务必须在 2 vCPU / 4 GiB RAM 上运行，不在本地运行模型。
- 生产环境关闭明文 HTTP、关闭数据库公网访问、限制管理端口。
- 凭据只允许通过环境变量、受限权限文件或服务端密钥挂载注入。
- 任何外部来源都必须设置 DNS、连接、读取和总超时。
- 单个来源失败不应阻塞其他来源，但本次运行应标记为部分失败。
- 所有外部文本作为不可信数据处理，不得覆盖系统规则或提示词。

## 10. 成功标准

### 10.1 判级正确性

- A 级测试集中的明确未来额度承诺全部被识别。
- 只有 `reset`、产品发布、模型发布、已发生重置和无来源猜测的测试样本全部不通知。
- 预测站高概率但无官方额度信号的样本全部不通知。
- 官方明确承诺与预测站低概率冲突时仍产生官方优先预警，并记录冲突。
- B→A 升级只产生一次升级通知。

### 10.2 运行正确性

- 08:00—23:00 每小时运行，00:00—07:59 不产生计划运行。
- 08:00 能补查前一晚 00:00—08:00 的新增内容。
- 失败运行不推进成功游标，重试后不会漏掉窗口内新增内容。
- 相同内容重复抓取不会重复通知。

### 10.3 用户体验

- 首次安装到完成配对不超过 2 分钟。
- 用户打开首页后 3 秒内能看到当前状态、上次检查和下次检查。
- 点击通知后能打开对应证据详情。
- 正常后台/退出 App 时能收到通知；系统强制停止或通知权限关闭时显示明确诊断。
- 手机窄屏、横屏和字体放大后不出现横向溢出或不可点击按钮。

## 11. 非目标

- 不做 iOS App；未来可复用 Rust 核心和 API，但本版本不实现 iOS UI。
- 不做 Gmail 登录、Google 登录、账号注册、多用户、团队权限或公开注册。
- 不做 X 官方 API 接入；本版本使用公开网页/公开搜索可访问内容的抓取适配器。
- 不监控用户个人 Codex 账户额度，不读取用户账户内部 API，不保证个人额度同步时间。
- 不使用本地大模型，不在服务器运行 GPU 推理。
- 不监控三个来源之外的新闻、社区、社交账号或第三方状态聚合站。
- 不发送“无预警”通知。
- 不把已经发生的重置当作未来预警。
- 不允许用户在 App 内修改 A/B 判级规则、预测阈值或来源列表。
- 不上架 Google Play，不实现商店支付、订阅和多渠道分发。

## 12. 已确认默认值

| 项目 | 默认值 |
|---|---|
| Android 包名 | `com.xingheluqi.codexresetwatch` |
| 时区 | `Asia/Shanghai` |
| 计划窗口 | 每日 08:00—23:00，每小时一次 |
| 08:00 补查 | 前一自然日 00:00:00 至当天 08:00:00 |
| 历史保留 | 30 天 |
| 语义模型 | `gpt-5.6-terra` |
| 设备数 | 1 |
| 配对码 | 8 位大写字母/数字，10 分钟有效，单次使用 |
| API 地址 | `https://<fixed-public-ip>/api` |
| 数据库 | SQLite |
| 主题 | Material 3 深色主题，灰黑分层表面，淡紫主色 |
| 发送人 | `xingheluqi56@gmail.com` |
| 收件人 | `2331613886@qq.com` |
| 安装方式 | 直接安装签名 APK |
